# 跨端契约记录

## 请求

- [ ] HTTP 方法与完整 `/api/v1` 路径：
- [ ] Path、Query、Body、FormData、分页和排序：
- [ ] 请求 DTO、默认值、可空性、枚举和校验：

## 响应

- [ ] 成功结构、分页、空值和时间/数值语义：
- [ ] 错误状态、错误码、消息和冲突语义：
- [ ] JSON 包装、文件、流、下载和上传行为：

## 安全与可靠性

- [ ] 登录态、刷新、权限码、租户、数据范围和资源所有权：
- [ ] 重复提交、幂等、并发、事务、重试、缓存和回滚：
- [ ] 后端 DTO/测试与前端类型/Parser/调用方已同步：
