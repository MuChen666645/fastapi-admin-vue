# 全栈功能交付检查表

## 后端

- [ ] Controller、DTO、Service、DAO 分层清晰，认证、权限、租户和数据范围在服务端强制校验。
- [ ] 菜单、权限 seed、迁移、初始化 SQL、任务处理器和审计变更已同步。
- [ ] 空数据、非法输入、无权限、跨租户、冲突、重复提交、事务失败和重试语义已测试。

## 前端

- [ ] API 类型、Parser、Store、页面和路由使用真实后端契约。
- [ ] 可见、隐藏、禁用、Loading、错误、成功和 handler 防护构成完整状态契约。
- [ ] 动态路由、权限指令、标签、缓存、内容区 Loading 和系统设置边界没有回归。

## 交付

- [ ] 两端目标测试和 `git diff --check` 已通过。
- [ ] 构建、迁移 SQL、Docker、MySQL、Redis、浏览器和第三方验证分别报告。
