# 前端 Codex 入口

修改 `frontend/` 下的任何文件前，必须按以下顺序阅读项目规则：

1. `.codex/AGENTS.md`：强制执行的实现、架构和边界规则。
2. `.codex/project-context.md`：当前前端结构和后端接口契约事实。
3. `.codex/security-boundary.md`：鉴权、权限、浏览器数据、文件和不可信配置规则。
4. `.codex/checklist.md`：开发验证和交付检查清单。

`.codex/` 下的文件是前端项目维护的 Codex 规范源。架构或后端契约发生确认过的变化时，必须同步更新这些文件。
